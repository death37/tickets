{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouveau Ticket</h1>

        {{ form_start(form) }}

                {% form_theme form 'jquery.collection.html.twig'
                                   'formThemeButtonCollection.html.twig' %}
                {{ form_widget(form) }}
                
        <input type="submit" value="Valider" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('tickets_index') }}">Back to the list</a>
            </li>
        </ul>
    
{% endblock %}
