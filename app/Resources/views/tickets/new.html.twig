{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouveau Ticket</h1>
    
{{ dump() }}
        {{ form_start(form) }}

        
{#        {{ form_row(form.title, { 'label': 'titre'}) }}
        {{ form_row(form.state, { 'label': 'Etat'}) }}#}

        
                {% form_theme form.images 'jquery.collection.html.twig'
                                             'formThemeButtonCollection.html.twig' %}
                
                {{ form_widget(form.images, { 'label': 'image'}) }}

            
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('tickets_index') }}">Back to the list</a>
            </li>
        </ul>
    
{% endblock %}
